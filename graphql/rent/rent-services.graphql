query GetAddRentServices($id: ID) {
  rentCar(id: $id) {
    data {
      id
      attributes {
        additionalServices {
          id
          label
          dailyPrice
        }
      }
    }
  }
}
